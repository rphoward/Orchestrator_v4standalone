# Infrastructure (`orchestrator_v4/infrastructure/`)

**Cursor source of truth:** `.cursor/rules/orchestrator-architecture.mdc` (always) and `.cursor/rules/orchestrator-layer-infrastructure.mdc` (globs: `orchestrator_v4/infrastructure/**`).

This file keeps the **`GEMINI.md` legacy filename** for Antigravity compatibility. Layer rules are not duplicated here—see **`AGENTS.md`** section 6 (legacy agent docs).
