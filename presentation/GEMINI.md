# Presentation (`orchestrator_v4/presentation/`)

**Cursor source of truth:** `.cursor/rules/orchestrator-architecture.mdc` (always) and `.cursor/rules/orchestrator-presentation.mdc` (globs: `presentation/**`).

This file keeps the **`GEMINI.md` legacy filename** for Antigravity compatibility. Layer rules are not duplicated here—see **`AGENTS.md`** section 6 (legacy agent docs).
