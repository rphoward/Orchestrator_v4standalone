# Inner Circle (`orchestrator_v4/core/`)

**Cursor source of truth:** `.cursor/rules/orchestrator-architecture.mdc` (always) and `.cursor/rules/orchestrator-layer-core.mdc` (globs: `orchestrator_v4/core/**`).

This file keeps the **`GEMINI.md` legacy filename** for Antigravity compatibility. Layer rules are not duplicated here—see **`AGENTS.md`** section 6 (legacy agent docs).
